# 客户经理电脑敏感文件自查工具 — V1 执行方案

- **文档版本**：spec-version1
- **日期**：2026-07-30
- **状态**：待评审

---

## 1. 项目背景与目标

### 1.1 背景

个贷客户经理在日常业务中，电脑内常会留存客户敏感资料：身份证照片、手机号、身份证号、还款流水、房产证及各类扫描件（图片或 PDF）。按合规要求需定期清理。现行做法是依照《员工电脑自查流程》，由客户经理手工使用 Everything 按敏感词和文件后缀搜索并自行处理，存在三个问题：

1. 部分客户经理电脑未安装 Everything，流程无法执行；
2. 约 30% 的信创电脑（麒麟 / 统信，Linux 内核）上 Everything 根本不可用，自查存在合规空白；
3. 手工流程无留痕，无法证明"已完成自查"。

### 1.2 目标

开发一款绿色便携的自查工具，替代手工 Everything 流程：

- **解压即用**：不需要安装、不需要管理员权限、不写注册表；
- **全平台覆盖**：一套代码同时覆盖 Windows 10 与信创电脑；
- **安全兜底**：命中文件先移入"待删除暂存区"，由客户经理人工确认后再删除，支持一键还原，杜绝误删事故；
- **合规留痕**：每次自查自动生成报告，替代纸质流程的完成证明；
- **单机离线**：全程不联网、不上传任何数据。

### 1.3 量化指标

| 指标 | 目标值 |
|------|--------|
| 部署方式 | 解压即用，单文件可执行程序 |
| 全盘扫描耗时 | 普通办公机 ≤ 5 分钟（含进度展示） |
| 权限要求 | 普通用户权限，无需管理员 |
| 网络要求 | 零联网，纯本机运行 |
| 误删风险 | 删除前必经暂存区 + 人工二次确认，可整单还原 |

---

## 2. 适用范围与运行环境

### 2.1 目标机型（分发 3 个包）

| 平台 | 系统 | CPU 架构 | Go 编译目标 |
|------|------|----------|-------------|
| Windows 办公机（约 70%） | Windows 10 x64 | x86-64 | `windows/amd64` |
| 信创办公机 | 麒麟 V10 / 统信 UOS | 海光、兆芯（x86-64） | `linux/amd64` |
| 信创办公机 | 麒麟 V10 / 统信 UOS | 飞腾、鲲鹏（ARM64） | `linux/arm64` |

> 如后续确认存在龙芯机型，追加 `linux/loong64` 目标（Go ≥ 1.19 官方支持），架构不变。

### 2.2 浏览器基线

- **兼容基线：Chromium 90**（客户经理因业务系统需要普遍安装 Chrome，最低约 90 版）；
- 信创机自带浏览器（奇安信、统信、麒麟浏览器）均为 Chromium 内核，同一套页面通用；
- 前端构建目标锁定 `chrome >= 90`，禁用 2021 年后新特性（如 `:has()` 选择器、顶层 await 直出产物）。

### 2.3 权限约束

- 全程普通用户权限。不读 NTFS MFT（需管理员），采用多线程目录遍历；
- 无法访问的目录（权限不足）静默跳过并计数，报告中注明跳过数量。

---

## 3. V1 功能清单

### 3.1 做什么

| 编号 | 功能 | 说明 |
|------|------|------|
| F1 | 规则化扫描 | 按"文件名敏感词 + 文件后缀"双维度扫描指定根目录，规则外置可配置 |
| F2 | 扫描进度 | 实时展示已扫目录数、已发现文件数、耗时；可中途取消 |
| F3 | 结果清单 | 按命中原因分组展示（敏感词命中 / 后缀命中 / 重点目录命中），全选 / 反选 / 逐条勾选 |
| F4 | 移入暂存区 | 勾选文件移动至"待删除暂存区"，写入 manifest 记录原路径与命中规则 |
| F5 | 一键还原 | 暂存区内任意文件可还原至原路径（含原路径已存在的冲突处理） |
| F6 | 确认删除 | 暂存区文件经二次确认后删除（Windows 进回收站，Linux 移入用户回收站目录） |
| F7 | 自查报告 | 生成本次自查报告（扫描范围、命中数、处理数、时间戳），导出为 HTML 单文件 |
| F8 | 完成退出 | 页面"完成并退出"按钮 + 心跳超时自动退出进程 |

### 3.2 明确不做（留给后续版本）

- ❌ 文档内容识别（身份证号 / 手机号 / 银行卡号正则）→ V2；
- ❌ 图片 OCR 识别证件照 → V3；
- ❌ 联网上报、集中管理后台 → 视合规需求另立项；
- ❌ 定时任务 / 开机自启 → V3 视需要；
- ❌ 老格式 `.doc` / `.xls` 内容解析（V1 仅按文件名与后缀命中）。

---

## 4. 总体架构

### 4.1 形态

单个可执行文件 = **Go HTTP 服务 + `embed` 内嵌前端静态页面**。

启动流程：

```
双击可执行文件
  → 绑定 127.0.0.1 随机可用端口，生成一次性随机 token
  → 调起系统默认浏览器打开 http://127.0.0.1:<port>/?token=<token>
  → 客户经理在网页完成 扫描 → 勾选 → 暂存 → 确认删除 → 导出报告
  → 点击"完成并退出"或心跳超时（默认 10 分钟无页面心跳）→ 进程自动退出
```

### 4.2 模块划分

```
manager-self-check/
├── cmd/selfcheck/main.go        # 入口：端口/token、打开浏览器、优雅退出
├── internal/
│   ├── server/                  # HTTP 路由、token 中间件、心跳
│   ├── rules/                   # rules.json 加载、默认规则、校验
│   ├── scanner/                 # 并发目录遍历、规则匹配、进度上报、取消
│   ├── quarantine/              # 暂存区：移动、manifest 读写、还原、删除
│   ├── report/                  # 自查报告生成（HTML 模板）
│   └── platform/                # 平台差异：默认扫描根、回收站、打开浏览器
├── web/                         # 前端源码（构建产物经 go:embed 打包）
│   └── dist/                    # 内嵌的静态文件
├── rules.json                   # 外置规则（可选，缺省时用内置默认规则）
└── build/                       # 三平台打包脚本
```

### 4.3 关键技术点

- 扫描器：`filepath.WalkDir` + worker 池（默认 4 并发），按扫描根并行；
- 前端与后端通信：REST + 进度轮询（每 500ms 拉一次 `/api/scan/progress`，避免 WebSocket 增加复杂度）；
- Windows 打包：`-ldflags "-H windowsgui"` 隐藏控制台黑框；
- Linux 打包：附带 `启动自查工具.desktop` 与 `start.sh`，双击可运行。

---

## 5. 扫描规则设计

### 5.1 rules.json 格式

规则外置为程序同目录的 `rules.json`，便于合规部门更新敏感词而不重新发版；文件缺失时使用内置默认规则（内容与下方一致）。

```json
{
  "version": "202607",
  "keywords": ["身份证", "宽表", "证", "合同", "执照", "手机", "地址",
                "流水", "房产", "征信", "授信", "客户"],
  "extensions": [".doc", ".docx", ".xls", ".xlsx", ".csv",
                  ".pdf", ".png", ".jpg", ".jpeg", ".bmp", ".zip", ".rar"],
  "hotDirs": {
    "windows": ["%USERPROFILE%\\Desktop", "%USERPROFILE%\\Downloads",
                 "%USERPROFILE%\\Documents", "%USERPROFILE%\\Documents\\WeChat Files",
                 "%USERPROFILE%\\Documents\\Tencent Files"],
    "linux":   ["~/桌面", "~/下载", "~/文档", "~/Desktop", "~/Downloads", "~/Documents"]
  },
  "scanRoots": {
    "windows": ["%USERPROFILE%", "D:\\", "E:\\", "F:\\"],
    "linux":   ["~", "/media", "/mnt"]
  },
  "excludeDirs": {
    "windows": ["C:\\Windows", "C:\\Program Files", "C:\\Program Files (x86)",
                 "%USERPROFILE%\\AppData"],
    "linux":   ["/proc", "/sys", "/usr", "/opt", "/var", "~/.cache", "~/.config"]
  },
  "minImageSizeKB": 30,
  "maxFileSizeMB": 500
}
```

### 5.2 命中逻辑

文件满足任一条件即列入结果，并标注命中原因（可多个）：

1. **敏感词命中**：文件名（不含路径）包含任一 `keywords`；
2. **后缀命中**：扩展名在 `extensions` 中，且文件位于 `hotDirs` 热点目录树内；
3. **重点提示**：同时满足 1+2 的标记为"高风险"，结果页默认置顶并预勾选；仅满足 2 的默认不预勾选（减少误判负担）。

补充规则：

- 图片类小于 `minImageSizeKB`（默认 30KB）跳过（图标、表情包噪音）；
- 大于 `maxFileSizeMB` 的文件仍列出但标注"大文件"，移动前提示耗时；
- 暂存区目录自身、本程序目录永远排除。

---

## 6. 暂存区与 manifest 设计

### 6.1 目录结构

暂存区固定位于用户主目录，跨平台一致：

```
<用户主目录>/待删除文件区/
├── manifest.json                 # 台账（唯一可信源）
├── 20260730-143022/              # 每次"移入暂存"操作一个批次目录
│   ├── 0001_身份证正面.jpg
│   ├── 0002_张三借款合同.pdf
│   └── ...
└── reports/
    └── 自查报告-20260730.html
```

- 批次目录以时间戳命名；文件加序号前缀，**物理上杜绝同名冲突**；
- 原始文件名与原始路径只记录在 manifest 中，不依赖文件名还原。

### 6.2 manifest.json 字段

```json
{
  "entries": [
    {
      "id": "20260730-143022-0001",
      "batch": "20260730-143022",
      "storedName": "0001_身份证正面.jpg",
      "originalPath": "C:\\Users\\wang\\Desktop\\身份证正面.jpg",
      "sizeBytes": 245760,
      "modifiedAt": "2023-12-20T17:29:00+08:00",
      "matchedRules": ["keyword:身份证", "ext:.jpg@hotdir"],
      "movedAt": "2026-07-30T14:30:22+08:00",
      "status": "pending"
    }
  ]
}
```

`status` 状态机：`pending`（待确认）→ `restored`（已还原）| `deleted`（已删除）。已终态条目保留在 manifest 中作为报告数据源。

### 6.3 还原与删除逻辑

- **还原**：按 `originalPath` 移回；若原路径已存在同名文件，还原为 `原名(还原-时间戳).后缀` 并在界面提示；若原目录已不存在则重建；
- **删除**：Windows 调用 Shell API 移入回收站（不直接物理删除）；Linux 按 FreeDesktop 规范移入 `~/.local/share/Trash`；均需页面二次确认弹窗（显示文件数与总大小）；
- **移动跨盘处理**：同盘用 rename（瞬时），跨盘退化为 copy + 校验大小 + 删源，失败则保留源文件并报错，保证任何情况下文件不丢失。

---

## 7. API 接口清单

所有接口要求请求头携带 `X-Token`（启动时生成的一次性 token），仅接受 `127.0.0.1` 来源。

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/scan` | 启动扫描（body 可覆盖扫描根）；已在扫描中则返回 409 |
| GET  | `/api/scan/progress` | 进度：已扫目录数 / 发现数 / 耗时 / 状态（running, done, cancelled） |
| POST | `/api/scan/cancel` | 取消当前扫描 |
| GET  | `/api/results` | 扫描结果列表（含命中原因、大小、修改时间、风险级别） |
| POST | `/api/quarantine` | 将勾选文件移入暂存区，body 为文件路径数组，返回批次号与逐条结果 |
| GET  | `/api/quarantine` | 暂存区台账（manifest 中 status=pending 的条目） |
| POST | `/api/restore` | 还原指定条目（body 为 id 数组） |
| POST | `/api/delete` | 删除指定条目（body 为 id 数组），服务端再次校验状态 |
| GET  | `/api/report` | 生成并返回本次自查报告 HTML，同时落盘至 reports/ |
| POST | `/api/heartbeat` | 页面心跳（前端每 30s 调一次） |
| POST | `/api/exit` | 完成并退出：优雅关闭 HTTP 服务并结束进程 |

---

## 8. 页面设计（3 页）

### P1 扫描页（首页）

- 显示本次扫描范围（来自规则文件）与规则版本号；
- "开始自查"大按钮 → 进度条（已扫目录数 / 发现文件数 / 耗时）+ "取消"；
- 完成后自动跳转结果页。

### P2 结果页

- 顶部统计条：共发现 N 个，高风险 M 个；
- 列表按风险分组：**高风险（预勾选）** / 敏感词命中 / 热点目录后缀命中；
- 每行：勾选框、文件名、完整路径（可点击复制）、大小、修改时间、命中原因标签；
- 支持按修改时间 / 大小排序（对齐现有纸质流程习惯）、按后缀筛选；
- 底部操作：全选、反选、"移入待删除暂存区（N 个）"。

### P3 暂存区页

- 待确认列表（同结果页行样式）+ 每行"还原"按钮；
- 底部操作：全部还原、**确认删除（二次确认弹窗，显示件数与总大小）**、"导出自查报告"、"完成并退出"；
- 空状态提示："暂存区已清空，本次自查完成，请导出报告留存。"

### 前端技术约束

- Vue 3 + Vite（构建目标 `chrome90`）或原生 HTML+JS（如追求极简）；不引入重型组件库；
- 全部资源经 `go:embed` 内嵌，页面零外部请求（内网无 CDN）；
- 界面中文，字号偏大（一线人员使用场景）。

---

## 9. 安全设计

1. **仅回环监听**：HTTP 服务绑定 `127.0.0.1`，绝不绑定 `0.0.0.0`；
2. **随机端口 + 一次性 token**：token 32 字节随机，经 URL 传入后由前端存入内存并放请求头；无 token / 错误 token 一律 401；
3. **防 CSRF**：校验 `Origin`/`Referer` 必须为本服务地址；API 一律 POST+JSON（简单表单跨站无法构造）；
4. **文件操作边界**：所有移动 / 还原 / 删除仅允许操作扫描结果与 manifest 中登记的路径，拒绝任意路径参数直传（防路径遍历）；
5. **不物理删除**：删除动作只进回收站，最后一道保险留给系统回收站；
6. **零数据外发**：无任何出站网络请求；报告只落本地磁盘；
7. **日志**：仅本地写运行日志（不含文件内容），供排障。

---

## 10. 打包与分发

### 10.1 产物命名

```
自查工具-v1.0.0-windows-x64.zip      # Win10（70% 机型）
自查工具-v1.0.0-信创-x64.tar.gz      # 麒麟/统信 + 海光/兆芯
自查工具-v1.0.0-信创-arm64.tar.gz    # 麒麟/统信 + 飞腾/鲲鹏
```

每个包内含：可执行文件、`rules.json`、`使用说明.txt`（三步：解压 → 双击 → 按页面提示操作；并说明如何判断自己该下哪个包）。

### 10.2 构建要点

- Windows：`GOOS=windows GOARCH=amd64 go build -ldflags "-H windowsgui -s -w"`；
- 信创：`GOOS=linux GOARCH=amd64|arm64`，附 `start.sh` 与 `.desktop` 文件并预设可执行位；
- 程序启动时自检平台，如包与机型不匹配（如 x64 包在 ARM 机上）给出中文提示；
- 版本号 `v主.次.修订`，规则文件独立版本号（`rules.json` 的 `version` 字段），报告中同时打印两者。

### 10.3 分发渠道

沿用现有 Everything 的分发方式：内网邮箱 / 内网共享盘发布压缩包。

---

## 11. 开发里程碑

| 阶段 | 内容 | 交付物 | 预估 |
|------|------|--------|------|
| M0 | **管控软件白名单申请**（与开发并行，立即启动） | 信息安全部门受理记录 | 视流程，最长前置项 |
| M1 | 扫描引擎：规则加载、并发遍历、命中逻辑、进度/取消（CLI 形态先行验证） | 可命令行运行的扫描器 + 单元测试 | 1 周 |
| M2 | 暂存区：移动 / manifest / 还原 / 回收站删除（含跨盘、冲突、失败保源用例） | 完整后端 + 测试 | 1 周 |
| M3 | Web UI 三页 + API 联调 + 报告导出 + 退出机制 | Windows 版可用完整工具 | 1–1.5 周 |
| M4 | 信创适配：x64 / ARM64 实机验证（回收站、浏览器调起、中文目录） | 三平台安装包 | 0.5–1 周（依赖借测机器） |
| M5 | 试点：选 1–2 个网点各平台试用，收集误判反馈调规则 | 修订版 rules.json + v1.0 正式包 | 1–2 周 |

关键依赖：M0 白名单与 M4 信创测试机需尽早协调，均不受开发进度控制。

---

## 12. 风险与应对

| 风险 | 等级 | 应对 |
|------|------|------|
| 终端管控软件拦截（陌生 exe + 批量文件操作 + 监听端口） | **高** | M0 提前报备加白；必要时申请代码签名证书；试点前在装有管控软件的真实办公机验证 |
| 误判打扰（命中太多正常工作文件，客户经理弃用） | 高 | 分级预勾选策略（仅高风险预勾选）；试点期专门收集误判样本调整 keywords/hotDirs；规则热更新不发版 |
| 信创机型碎片化（浏览器调起失败、回收站路径差异） | 中 | platform 层隔离差异；调起浏览器失败时页面地址打印在窗口/日志中供手动打开；M4 实机全流程回归 |
| 扫描慢（老机器 + 机械盘 + 海量小文件） | 中 | worker 池 + 排除目录裁剪；进度实时可见 + 可取消；超时可只扫热点目录（降级模式） |
| 客户经理误关进程导致操作中断 | 低 | manifest 每次操作即时落盘，重启程序自动恢复暂存区状态；扫描结果丢失可重扫 |

---

## 13. V1 验收标准

1. 在 Win10 与两类信创实机上：解压 → 双击 → 浏览器自动打开页面，全流程（扫描 → 勾选 → 暂存 → 还原 → 删除 → 报告）无需任何安装与管理员权限；
2. 制造 20 个样本文件（含敏感词命中、热点目录后缀命中、同名冲突、跨盘、大文件），全部按第 5/6 章规则正确处理，无文件丢失；
3. 断电 / 强杀进程后重启，暂存区与 manifest 状态一致，可继续操作；
4. 端口仅监听 127.0.0.1，错误 token 返回 401（用 curl 验证）；
5. 全程抓包无任何出站请求。
